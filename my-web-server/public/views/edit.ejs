<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sửa sản phẩm</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Sửa sản phẩm</h1>
    <form action="/update/<%= id %>" method="POST" class="row g-3">
      
      <!-- Tên sản phẩm -->
      <div class="col-md-6">
        <label for="tense" class="form-label">Tên sản phẩm:</label>
        <input type="text" id="tense" name="tense" value="<%= product.tense %>" class="form-control" required>
      </div>

      <!-- Giá tiền -->
      <div class="col-md-6">
        <label for="giatien" class="form-label">Giá tiền:</label>
        <input type="number" id="giatien" name="giatien" value="<%= product.giatien %>" class="form-control" required>
      </div>

      <!-- Loại sản phẩm -->
      <div class="col-md-6">
        <label for="loaisp" class="form-label">Loại sản phẩm:</label>
        <input type="text" id="loaisp" name="loaisp" value="<%= product.loaisp %>" class="form-control" required>
      </div>

      <!-- Mô tả -->
      <div class="col-md-6">
        <label for="mota" class="form-label">Mô tả:</label>
        <textarea id="mota" name="mota" class="form-control" rows="3" required><%= product.mota %></textarea>
      </div>

      <!-- Hạn sử dụng -->
      <div class="col-md-6">
        <label for="hansudung" class="form-label">Hạn sử dụng:</label>
        <input type="text" id="hansudung" name="hansudung" value="<%= product.hansudung %>" class="form-control" required>
      </div>

      <!-- Số lượng -->
      <div class="col-md-6">
        <label for="soluong" class="form-label">Số lượng:</label>
        <input type="number" id="soluong" name="soluong" value="<%= product.soluong %>" class="form-control" required>
      </div>

      <!-- Trọng lượng -->
      <div class="col-md-6">
        <label for="trongluong" class="form-label">Trọng lượng:</label>
        <input type="number" id="trongluong" name="trongluong" value="<%= product.trongluong %>" class="form-control" required>
      </div>

      <!-- Hình ảnh -->
      <div class="col-md-6">
        <label for="hinhanh" class="form-label">Hình ảnh (URL):</label>
        <input type="text" id="hinhanh" name="hinhanh" value="<%= product.hinhanh %>" class="form-control" required>
      </div>

      <!-- Nút cập nhật -->
      <div class="col-12 text-center">
        <button type="submit" class="btn btn-primary">Cập nhật</button>
      </div>
    </form>
  </div>

  <!-- Bootstrap JS (Optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
