<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa Booking</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container mt-5">
        <h1 class="text-center mb-5">Cập nhật Trạng Thái Booking</h1>
        
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="/bookings">Booking</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <!-- Sửa trạng thái booking -->
        <div class="card">
            <div class="card-header bg-success text-white">
                Sửa Trạng Thái của Booking: <%= booking.idcthdbooking %>
            </div>
            <div class="card-body">
                <form action="/bookings/editBooking/<%= booking.idcthdbooking %>" method="POST">
                    <div class="mb-3">
                        <label for="trangThai" class="form-label">Trạng Thái</label>
                        <select class="form-select" id="trangThai" name="trangThai" required>
                            <option value="Đã xác nhận" <%= booking.trangThai === 'Đã xác nhận' ? 'selected' : '' %>>Đã xác nhận</option>
                            <option value="Đã huỷ" <%= booking.trangThai === 'Đã huỷ' ? 'selected' : '' %>>Đã huỷ</option>
                            <option value="Chưa xác nhận" <%= booking.trangThai === 'Chưa xác nhận' ? 'selected' : '' %>>Chưa xác nhận</option>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">Cập nhật Trạng Thái</button>
                </form>
            </div>
        </div>

        <a href="/bookings" class="btn btn-secondary mt-4">Quay lại</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
