<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách sản phẩm</title>
  <!-- Thêm Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Danh sách sản phẩm</h1>

    <!-- Form thêm sản phẩm -->
    <div class="card mb-4">
      <div class="card-body">
        <h5 class="card-title">Thêm sản phẩm mới</h5>
        <form action="/add" method="POST">
          <div class="row g-3">
            <div class="col-md-6">
              <label for="tense" class="form-label">Tên sản phẩm</label>
              <input type="text" class="form-control" id="tense" name="tense" placeholder="Nhập tên sản phẩm" required>
            </div>
            <div class="col-md-6">
              <label for="giatien" class="form-label">Giá tiền</label>
              <input type="number" class="form-control" id="giatien" name="giatien" placeholder="Nhập giá tiền" required>
            </div>
            <div class="col-md-6">
              <label for="loaisp" class="form-label">Loại sản phẩm</label>
              <input type="text" class="form-control" id="loaisp" name="loaisp" placeholder="Nhập loại sản phẩm" required>
            </div>
            <div class="col-md-6">
              <label for="hansudung" class="form-label">Hạn sử dụng</label>
              <input type="date" class="form-control" id="hansudung" name="hansudung" required>
            </div>
            <div class="col-md-6">
              <label for="soluong" class="form-label">Số lượng</label>
              <input type="number" class="form-control" id="soluong" name="soluong" placeholder="Nhập số lượng" required>
            </div>
            <div class="col-md-6">
              <label for="trongluong" class="form-label">Trọng lượng (kg)</label>
              <input type="number" class="form-control" id="trongluong" name="trongluong" placeholder="Nhập trọng lượng" required>
            </div>
            <div class="col-md-12">
              <label for="mota" class="form-label">Mô tả</label>
              <textarea class="form-control" id="mota" name="mota" rows="3" placeholder="Nhập mô tả sản phẩm" required></textarea>
            </div>
            <div class="col-md-12">
              <label for="hinhanh" class="form-label">Hình ảnh (URL)</label>
              <input type="text" class="form-control" id="hinhanh" name="hinhanh" placeholder="Nhập URL hình ảnh" required>
            </div>
          </div>
          <div class="mt-3">
            <button type="submit" class="btn btn-success">Thêm sản phẩm</button>
          </div>
        </form>
      </div>
    </div>

    <!-- Danh sách sản phẩm -->
    <div class="row">
      <% if (products && products.length > 0) { %>
        <% products.forEach(product => { %>
          <div class="col-md-4 mb-4">
            <div class="card shadow-sm">
              <div class="card-body">
                <h5 class="card-title"><%= product.tense %></h5>
                <p class="card-text"><strong>Giá:</strong> <%= product.giatien %> VNĐ</p>
                <p class="card-text"><strong>Loại:</strong> <%= product.loaisp %></p>
                <p class="card-text"><strong>Mô tả:</strong> <%= product.mota %></p>

                <div class="d-flex justify-content-between">
                  <a href="/edit/<%= product.id %>" class="btn btn-warning btn-sm">Sửa</a>
                  <form action="/delete/<%= product.id %>" method="POST" onsubmit="return confirm('Bạn có chắc chắn muốn xóa sản phẩm này?')" style="margin: 0;">
                    <button type="submit" class="btn btn-danger btn-sm">Xóa</button>
                  </form>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <div class="text-center">
          <p>Không có sản phẩm nào.</p>
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
